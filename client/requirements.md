## Packages
recharts | For dashboard analytics charts
date-fns | For date formatting and calendar calculations
framer-motion | For smooth page transitions and micro-interactions
clsx | For conditional class merging
tailwind-merge | For merging tailwind classes utility

## Notes
- Tailwind config needs to extend colors with the specific green palette requested.
- Font families: 'Outfit' for headers, 'Inter' for body.
- File upload handling assumes Base64 encoding as per schema 'fileData' text field.
- Calendar view will be built using date-fns grid logic.
